(import 'sys/func.inc)
(import 'sys/mail/mail.inc)
(import 'class/lisp/lisp.inc)

(def-func 'class/lisp/mail_mymail)
	;inputs
	;r0 = lisp object
	;r1 = args
	;outputs
	;r0 = lisp object
	;r1 = value

	(ptr 'this 'args 'value)
	(uint 'length)

	(push-scope)
	(f-entry 'lisp 'mail_mymail {this, args})

(errorcases
	(v-call 'vector 'get_length {args} {_, length})
	(errorif {length} 'error1))

	(f-call 'sys_mail 'mymail {} {args, _})
	(f-call 'sys_mail 'free_str {args} {value})

(vp-label 'exit)
	(f-exit 'lisp 'mail_mymail {this, value})
	(return)

(errorcases
(vp-label 'error1)
	(f-call 'lisp 'repl_error {this, "(mail-mymail)", error_msg_wrong_types, args} {_, value})
	(goto 'exit))

	(pop-scope-syms)

(def-func-end)
