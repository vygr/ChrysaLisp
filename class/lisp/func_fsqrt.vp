(import 'sys/func.inc)
(import 'sys/math/class.inc)
(import 'class/integer/class.inc)
(import 'class/lisp/class.inc)

(def-method 'lisp 'func_fsqrt)
	;inputs
	;r0 = lisp object (ptr)
	;r1 = args vector object (ptr)
	;outputs
	;r0 = lisp object (ptr)
	;r1 = return value object (ptr)

	(ptr 'this 'value)
	(union
		'(ptr 'args)
		'(long 'num))

	(push-scope)
	(f-entry 'lisp 'func_fsqrt {this, args})

(errorcases
	(call 'lisp 'env_args_type (cat {this, args, $sig, 1}) {_, value})
	(errorifnot {value} 'error1))

	(call 'vector 'get_first {args} {_, value})
	(call 'integer 'get_value {value} '(_ r0))
	(errorif '(r0 < 0) 'error1)
	(call 'sys_math 'fsqrt '(r0) {num})
	(call 'integer 'create {num} {value})

(vp-label 'exit)
	(f-exit 'lisp 'func_fsqrt {this, value})
	(return)

(errorcases
(vp-label 'error1)
	(call 'lisp 'repl_error {this, "(fsqrt num)", error_msg_wrong_types, args} {_, value})
	(goto 'exit)
	(signature 'sig 'integer))

	(pop-scope-syms)

(def-func-end)
