Asm_Kernel
==========

Assembler x86_64 multi tasking kernel. Currently for OSX/BSD but will eventually run on Linux too.

Allows modeling of various network topologies with point to point links.

Make with:

make

Run with:

./run.sh <num_cpus>

Stop with:

./stop.sh
