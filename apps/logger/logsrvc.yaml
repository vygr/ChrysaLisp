####################################################################
# ChrysaLisp Logging Configuration File
####################################################################

---

version: 1

logging:
  levels:
    debug:    0
    info:     1
    warning:  2
    error:    3
    critical: 4
    logstrs: [DEBUG, INFO, WARNING, ERROR, CRITICAL]
  formatters:
    standard:
      format: tnlm
  handlers:
    # Generic console handler
    console_handler:
      type: :stdout
      level: :info
      formatter: :standard
    # Generic application file handler
    app_file_handler:
      type: :file
      level: :debug
      formatter: :standard
      file_name: applog
      rotate: true
      maxbytes: 10485760
      backups: 10
    # System log handler
    system_handler:
      type: :file
      level: :info
      formatter: :standard
      file_name: syslog
      rotate: true
      maxbytes: 10485760
      backups: 10
  loggers:
    console:
      handler: :console_handler
    file:
      handler: :app_file_handler
  # Specific loggers
  # :root is use with default 'system_handler'
  root:
    handler: :system_handler
...
